# start R
To start R statistical programming 
language type R in the command line of the R4.5WS conda environment 

```{cmd}
R
```

# Install packages for Bioconductor
installing `BiocManager` form CRAN (comprehensive R Archival Network)
using `install.packages()` function R base. Please type the following,
```(R)
install.packages("BioManager")
```